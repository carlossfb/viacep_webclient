spring:
  main:
    allow-bean-definition-overriding: true

springdoc:
  swagger-ui:
    enabled: true


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    prometheus:
      enabled: true

sonar:
  projectKey: teste
  organization: carlossfb
  hostUrl: ${SONAR_HOST_URL}

server:
  address: 0.0.0.0

